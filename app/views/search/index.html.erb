<div class="bg-gray-700 text-columbia-blue px-3 py-2 border-l border-r border-gray-600">
  <% @search_results.each do |result| %>
    <div class="border-t border-b border-columbia-blue"><%= result.name %></div>
  <% end %>
</div>